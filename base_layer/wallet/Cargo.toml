[package]
name = "wallet"
version = "0.0.1"
edition = "2018"

[dependencies]
tari_core = { path = "../core", version="0.0.1"}
tari_crypto = { path = "../../infrastructure/crypto", version = "0.0.1" }
tari_utilities = { path = "../../infrastructure/tari_util", version = "0.0.1"}
derive-error = "0.0.4"
serde = {version = "1.0.89", features = ["derive"] }

[dev-dependencies]
rand = "0.5"



BUILD_ANDROID=0 for false, 1 for true
BUILD_IOS=0 for false, 1 for true
NDK_PATH=The ndk path
PKG_PATH=the zeromq pkgconfig path
ANDROID_WALLET_PATH=the android wallet path
IOS_WALLET_PATH=the ios wallet path
TARI_REPO_PATH=the local tari repo path
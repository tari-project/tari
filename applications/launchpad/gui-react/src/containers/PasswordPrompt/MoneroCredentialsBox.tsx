import { MoneroCredentials } from './types'

const MoneroCredentialsBox = ({
  onSubmit,
}: {
  onSubmit: (monero: MoneroCredentials) => void
}) => {
  return <p>monero credentials box</p>
}

export default MoneroCredentialsBox

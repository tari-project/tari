import { WalletParole, MoneroCredentials } from './types'

const AllCredentialsBox = ({
  onSubmit,
}: {
  onSubmit: (parole: WalletParole, monero: MoneroCredentials) => void
}) => {
  return <p>all credentials box</p>
}

export default AllCredentialsBox

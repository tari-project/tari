/**
 * General JSDoc for IconComponents:
 *
 * IconComponents created from exported Figma svg files using SVGR package
 *
 * Components accept both inherited styling (color and fontSize) from parent and all native HTML SVG element attributes, e.g. width, height, color, etc.
 *
 * Components are imported for use with the prefix 'Svg', <Svg*filename* />
 *
 * @example
 * <SvgAdd width={20} height={20} color={theme.icon.primary} />
 *
 * --OR--
 *
 * <SvgArrowRight />
 */

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface SvgIconProps {}
